# User-tester run — 2026-07-30 — full app

App: RestaurantPOS.WpfApp (net10.0-windows), build Debug.
DB: (localdb)\mssqllocaldb / RestaurantPOSDb, collation SQL_Latin1_General_CP1_CI_AS.
Test accounts created: cashier1/Cashier@123 (UserId 2), kitchen1/Kitchen@123 (UserId 3). Existing seed: admin/Admin@123 (UserId 1).
Driver: PowerShell + System.Windows.Automation (UIA) + SendKeys for PasswordBox. Script: scratchpad/ui-driver.ps1.

Legend: PASS / FAIL / BLOCKED / FLAKY

## Session 0 — Login (not logged in)

1. Login happy path (admin/Admin@123): not yet run standalone, confirmed indirectly via case-variant test below. PASS.
2. Wrong password (admin/WrongPass1): "Sai tên đăng nhập hoặc mật khẩu." shown, stays on Login. PASS.
3. Unknown username (nosuchuser/Whatever1): identical generic message, no leak of user-exists info. PASS.
4. Empty fields: "Vui lòng nhập tên đăng nhập và mật khẩu.", no AuthService call (per code). PASS.
5. Whitespace-only fields ("   "/"   "): same as empty — treated as blank. PASS.
6. Username case variant (ADMIN uppercase / Admin@123): login SUCCEEDED — confirms DB collation SQL_Latin1_General_CP1_CI_AS makes username lookup case-insensitive. Screenshot: post-admin-login.png (shows MainWindow + ShiftWindow gate with "Bỏ qua" skip button, "Xin chào, Administrator (Admin)"). Not a bug — architecture-known behavior, but worth documenting as intended-or-not for Boss. PASS (documented behavior).
7. Trailing-space password ("admin"/"Admin@123 "): generic wrong-password error — hash comparison is exact, space breaks it. PASS (matches code prediction).
8. Wrong password spammed 5x rapid: no lockout, no rate-limit, no delay increase — same generic error every time. PASS as "no defense" — confirms known gap, not itself a bug to report.
9. SQL injection string in username ("' OR 1=1--"): safely treated as unknown username, generic error, no crash, no leak (EF parameterized query). PASS.
10. 10,000-character username: no crash, no hang (app stayed Responding=True, ~1.5s round trip including UI settle), generic wrong-user error. PASS.
11. Double-click Login button (rapid, valid creds via UIA InvokePattern.Invoke() x2): **FAIL — bug found.** Result: 4 windows opened (2x "Restaurant POS" MainWindow + 2x "Ca làm việc" ShiftWindow gate) from a single login action. Root cause pinned via source read of `LoginWindow.xaml.cs:13-31` (independent of the click that triggered it, not just re-clicking): `Login_Click` disables the button (`button.IsEnabled = false`) at entry, but the `finally { button.IsEnabled = true; }` re-enables it again once the (fully synchronous, no async/await) handler finishes — including on the SUCCESS path, right as `Close()` is tearing the window down. A second already-queued click event (real fast double-click, or any automation/assistive-tech client using `InvokePattern.Invoke()`) lands on the now-re-enabled button and re-enters `Login_Click`, running the whole login+window-open sequence a second time. Severity: **major** — no data loss/security issue, but user ends up with two live MainWindow sessions sharing one static `SessionContext.CurrentUser`; closing/logging out of one likely affects the other unexpectedly. Caveat for the report: reliably reproduced via UIA double-invoke (fires both clicks back-to-back with no gap); reproducibility via a genuine two-finger-fast mouse double click depends on DB round-trip latency vs. OS double-click timing — worth a manual mouse-driven re-check by Boss to confirm real-world reach, but the code defect (guard doesn't survive the success path) is real regardless.
12. Enter key submits (IsDefault button): single Enter press → exactly one MainWindow + one ShiftWindow gate opened, no duplication (contrast with row 11 — a single input event doesn't trigger the race). PASS.

Menu visibility confirmed for Admin: all 7 items shown (Bàn ăn, Ca làm việc, Bếp, Thực đơn, Kho nguyên liệu, Báo cáo, Lịch sử ca). PASS.

## Session 1 Admin — Dashboard

Default range on open: 7/24/2026 – 7/30/2026 (today-6 .. today) — matches code (`ToDate.AddDays(-6)`). PASS.
13. Empty range (fresh DB, 0 payments/orders): chart renders with default 0-10 Y-axis, no bars; "Top món bán chạy" grid empty; "Sắp hết hàng" grid empty. No crash. PASS. Screenshot: dashboard-default.png.
14. Refresh clicked twice in a row: grids stay at their correct row count both times (0 and 0 here since no data) — `.Clear()` before repopulate confirmed not to duplicate. PASS.
15. From > To (Từ=7/24/2026, Đến=1/1/2020): no crash, no hang (app stayed Responding=True), Refresh silently returns empty grids/chart — **no validation/warning shown to the user that the range is nonsensical**. Minor/polish finding, not a crash: flag as "silently accepts an inverted date range" rather than blocking or warning. Screenshot: dashboard-fromgreaterthanto.png.
16. From=To=today boundary regression check: **PASS, confirmed with real data.** After completing 3 payments today (17:31–17:4x on 2026-07-30: 180,000đ Cash for T1, 0đ for T3, 45,000đ BankTransfer for T4), reopened Dashboard with the default 7/24–7/30 range and clicked Refresh. The revenue chart correctly shows a 225,000đ bar for "30/07" (180,000+45,000, the ₫0 payment contributing nothing to the total as expected), and "Top món bán chạy" correctly lists Chicken Rice (3 sold, 165,000), Spring Rolls (1, 45,000), Iced Tea (1, 15,000) — exactly matching what was ordered. If the `ToDate.AddDays(1).AddTicks(-1)` end-of-day boundary fix (`DashboardViewModel.cs:46`) had regressed back to using raw midnight, these same-day-afternoon payments would have been silently excluded. They were correctly included. Screenshot: dashboard-with-data.png.
17. Low-stock panel reacting to date filter: confirmed NOT date-filtered (per code) — panel stayed empty even when To was changed to 1/1/2020, consistent with `GetLowStockIngredients()` taking no date params. Not a bug, but confirms the UX gap already flagged in recon (date picker looks like it scopes the whole page, but doesn't touch this panel).

## Session 1 Admin — Inventory (recorded here first, run after Menu Management)

30. Add ingredient negative quantity ("Salt", -5): blocked, "Số lượng và ngưỡng cảnh báo không được âm." PASS.
31. Add ingredient negative threshold ("Salt", 5/-1): blocked, same message. PASS.
32. Add ingredient duplicate name case-insensitive ("chicken" vs "Chicken"): blocked, "Không thể thêm nguyên liệu — tên có thể đã tồn tại." Ingredient count stayed 5. PASS.
33. Add "Sugar" with QuantityInStock == LowStockThreshold == 2 (boundary): added successfully, "Sắp hết" checkbox showed **On** — confirms `<=` boundary behavior. PASS.
34. Delete "Sugar" (never used in any recipe): confirm dialog → Yes → deleted cleanly. PASS. (Also served as cleanup of test data.)
35. Delete "Tea Leaves" (used in the Iced Tea recipe): confirm dialog → Yes → blocked, "Không thể xóa — nguyên liệu đang được dùng trong món ăn.", ingredient still exists. PASS.
36. Receive stock happy path (Chicken +10): `QuantityInStock` 20→30, one new row in `IngredientStockEntries`. PASS.
37. **Concurrency edit shows wrong error message: FAIL — bug confirmed live.** Simulated a second user's concurrent write (raw SQL bumping Chicken's `RowVersion` between the Inventory window's last `Load()` and my UI-side update), then changed Chicken's threshold via the UI and clicked "Cập nhật". Result: update correctly rejected (DB unchanged — no data corruption, `LowStockThreshold` stayed 3.000), but the UI showed **"Không thể cập nhật nguyên liệu — tên có thể đã tồn tại."** (implies duplicate name) for what is actually a pure concurrency conflict — nonsensical here since the name wasn't even touched. Root cause pinned via source: `IngredientDAO.UpdateIngredient` (`IngredientDAO.cs:30-48`) has a dedicated `catch (DbUpdateConcurrencyException) { return false; }` block (so the DAO *does* distinguish the cause internally) but returns the same bare `false` as every other failure; `InventoryViewModel.Update` (`InventoryViewModel.cs:120-145`) then hardcodes the one "tên có thể đã tồn tại" message for any `false`. Independently confirmed via `error.log`: **no entry was written at all** for this failure (the concurrency catch block has no `AppLogger` call, unlike the generic `catch (Exception ex)` below it) — so this failure mode is invisible to log inspection too, not just mislabeled on screen. Severity: **minor** (no data loss — the reject is correct — but actively misleads the operator about what happened, and leaves zero audit trail). Same systemic pattern as Menu Management's truncation-mislabeled-as-duplicate finding (row 29) — evidently a repeated template across the DAOs (`catch { return false }` collapsing distinct failure causes into one generic UI message).

## Session 1 Admin — Shift

38. Open shift with negative OpeningCash (-100): **FAIL — confirmed live, matches recon gap.** No validation anywhere (ViewModel/Service/DAO) blocks a negative opening float. Shift #1 opened with `OpeningCash=-100`. The whole reconciliation view then displayed the nonsense baseline visibly to the user: "Tiền đầu ca: -100 đ", "Tiền dự kiến trong két: -100 đ" (screenshot: shift-opened-negative.png). Severity: **minor** (no crash, recoverable, but a fat-fingered negative/huge number corrupts that shift's whole cash-reconciliation math with zero warning).
39. Close shift with large mismatch (Chênh lệch: 100 đ here): closed successfully with no confirmation, no required reason — matches documented gap. Screenshot: shift-closed-summary.png shows the "Tóm tắt ca vừa đóng" panel rendering all fields correctly (reconciliation-on-close-summary feature, commit 1a21624) — confirms that recent feature itself works correctly; the bug is only the missing guard on the underlying negative input. PASS for the summary feature itself.
40. Print shift report (PDF): opened successfully in the default browser ("shift-report-1.pdf and 3 more pages"), app stayed responsive. PASS.
41. Double-open shift for the same user while one is already open: not re-triggered via a fresh UI click (the normal menu flow structurally prevents it — `ShowSingle<ShiftWindow>` means there's only ever one Shift window, and once a shift is open the "Mở ca" button is replaced by "Đóng ca"). Verified instead via the deployed schema: the filtered unique index `IX_Shifts_UserId ... WHERE ClosedAt IS NULL` is present (confirmed via `sys.indexes` query), backing the existing `ShiftServiceTests` coverage. PASS (verified structurally + at the DB level, not via a forced live double-click).
42. Live reconciliation refresh on window `Activated`: opened Shift (shift #2, OpeningCash 500,000), left it in the background, completed a 180,000đ cash payment in a separate Order/Payment window, then Alt-tabbed back to the Shift window. "Doanh thu tiền mặt" correctly updated to 180,000 đ and "Tiền dự kiến trong két" to 680,000 đ without any manual refresh click (screenshot: shift-live-reconciliation.png). PASS.
43. Logout with an open shift (shift #2, still open): clicking "Đăng xuất" showed "Xác nhận đăng xuất" ("Bạn đang trong ca làm việc. Đăng xuất sẽ không đóng ca. Tiếp tục?"). Clicked "No" first — logout cancelled, stayed on MainWindow (still logged in). Clicked "Đăng xuất" again → "Yes" — logout succeeded, back at LoginWindow. Confirmed the shift itself is untouched by logout (by design — logout ≠ close shift). PASS.

## Session 1 Admin — TableMap / Order / Payment

Table tiles (`TableMapWindow`) are a `Border` with a `MouseLeftButtonDown` handler, not a `Button` — no UIA `InvokePattern`. Had to drive these via a genuine simulated double left-click (two real `mouse_event` down/up pairs at the tile's screen coordinates) rather than the `InvokePattern.Invoke()` used everywhere else — noted here since it's a real double physical click, closing the gap flagged in row 11 (Session 0) about automation-only double-invoke.

44. T1: double-click a Free table → Order window opens, table flips to Occupied (`Status=1`), `Order` row created with `Status=Open`. PASS.
45. Add "Chicken Rice" x2 to cart, then add "Chicken Rice" x1 again: quantities merge into a single draft line of 3 (not two separate lines) — confirmed both visually (screenshot: order-cart-filled.png) and via total (180,000đ = 3×55,000 + 1×15,000 Iced Tea). PASS.
46. Send to kitchen (happy path): 2 `OrderItems` persisted (Chicken Rice x3, Iced Tea x1), both `Status=Pending`. PASS.
47. Insufficient-stock at send-to-kitchen: added Chicken Rice x150 to the draft (needs 45kg chicken vs. 30kg in stock) and sent — blocked with "Không đủ nguyên liệu cho các món này.", the oversized draft line was **not** persisexists (only the original 2 OrderItems remained), and `Ingredients.QuantityInStock` for Chicken stayed untouched at 30.000 (no partial deduction). Draft line stayed in the cart afterward so it could be corrected/removed rather than being wiped. PASS. (This also exercises the "available item that runs out of ingredient stock" class of case from the matrix — same code path.)
48. Send-to-kitchen with qty ≤ 0: **not reproduced via live UI** — the "Thêm vào giỏ" button's `CanExecute` requires quantity > 0, so the server-side guard (already unit-tested per recon, `OrderServiceTests`) isn't reachable through the normal UI flow. Not a gap — noting as verified-by-code/tests rather than live-repro.
49. Cancel without reason: clicking "Hủy đơn" with the reason field blank showed a native "Thiếu thông tin" MessageBox and did **not** call the service — order stayed Open. PASS.
50. Cancel with reason: order cancelled successfully, `Status=Cancelled`, `CancelReason` stored verbatim, table freed (`Status=Free`). PASS. (Did not separately verify the confirm-dialog-text variant for "order already has sent items" vs. draft-only — this specific order had 0 sent items at cancel time, so only the draft-only wording was exercised.)
51. **Close Order window via the X button with unsent draft items still in the cart: FAIL — confirmed live, matches recon-flagged risk.** Added "Beef Noodle Soup" x2 to the draft on T3, then closed the window via the title-bar Close button with zero prompt. Result: table T3 stayed **Occupied** (Status=1), the underlying `Order` row still exists (`Status=Open`) but has **0 `OrderItems`** — the draft was silently discarded, no data was actually lost (nothing had been persisted yet) but the table is now stuck Occupied with an invisible "phantom" empty order until someone reopens T3 and either adds real items or cancels it. No confirmation/unsaved-changes prompt at all, unlike `ShiftWindow`'s `Closing` guard which the codebase already has a pattern for elsewhere. Severity: **minor** (recoverable, no data loss in the strict sense, but a real staff-facing trap).
52. **Empty order checkout (0 items): FAIL — bug confirmed live.** Reopened T3's empty phantom order from row 51, clicked straight through to "Thanh toán" with 0 sent/draft items — Payment window opened showing "Tổng cộng: 0 đ", confirmed with `AmountTendered=0`, and the checkout **succeeded**: a real `Payment` row was created (`AmountPaid=0.0000`), `Order.Status` flipped to Paid, table freed. No guard anywhere blocks a zero-item "sale." Severity: **minor** (no financial loss — it's ₫0 — but it lets a table be "closed out" with a legitimate-looking Payment record and zero items/revenue, silently masking what should have been either a real order or an explicit cancel).
53. Underpay (Cash, `AmountTendered=0` on a 180,000đ order): blocked client-side, "Tiền khách đưa không đủ." PASS.
54. Overpay (Cash, `AmountTendered=999,999,999,999` on a 180,000đ order): accepted with no upper bound, `ChangeDue` displayed as 999,999,819,999 đ, no crash. Confirms the documented gap (no sanity cap on tendered amount) — minor/polish, not blocking.
55. Exact payment (Cash, `AmountTendered=180,000` on a 180,000đ order): checkout succeeded — `Payment` created (Method=Cash), `Order.Status=Paid`, table freed, ingredients correctly deducted (Chicken 30→29.1, Rice 30→29.4, Tea Leaves 5→4.98 — math verified exactly against the recipe quantities). "In hóa đơn" (print receipt) opened a real PDF in the default browser with no crash. PASS — this is also the real transaction the deferred Dashboard boundary-date check (Session 1 Admin — Dashboard, row 16) needed.
56. BankTransfer with `AmountTendered` left at 0 (untouched): "Xác nhận thanh toán" stayed enabled and checkout **succeeded** — `Payment` created (Method=BankTransfer, correct order total). Confirms BankTransfer applies no amount-tendered validation at all, unlike Cash. Matches documented design difference, not flagged as a new bug — but combined with row 54/52, the Payment flow overall has very little sanity-checking outside the one Cash-underpay guard.
57. Checkout-time stock race (two orders competing for the last unit of an ingredient, one passes send-to-kitchen and then loses at checkout): **not reproduced live** — would need two concurrent Order/Payment sessions racing the same ingredient, out of reach of a single UI-automation thread in the time available. Relying on the documented design (`PaymentDAO.CheckoutOrder` re-checks and accumulates required ingredients before deducting, all-or-nothing in one transaction) and the equivalent already-tested logic at the send-to-kitchen layer (row 47). Flagging as **not tested**, not as pass/fail.

## Session 1 Admin — Kitchen

58. Normal queue display: created a new order on T5 (Beef Noodle Soup x2), sent to kitchen — appeared in the "Màn hình bếp" queue as "Bàn T5 — Beef Noodle Soup x2 / Pending" with "Đang làm"/"Xong" buttons. PASS.
59. Click "Xong" (Done) directly on a `Pending` item, skipping "Đang làm" (InProgress): **confirmed, no guard** — `OrderItemId=4` jumped straight from `Status=0` (Pending) to `Status=2` (Done). No state-machine validation blocks this in `OrderDAO.UpdateOrderItemStatus`. Classifying as a **product-intent question for Boss** rather than a hard bug — it may be intentionally flexible (kitchen staff sometimes mark a quick item done without a separate "started" click), but worth confirming whether skipping InProgress should be disallowed.

## Session 2 — Cashier (cashier1/Cashier@123)

64. Login as Cashier: forced shift gate shown with **no "Bỏ qua" (Skip) button** — only "Mở ca" and a red "Thoát ứng dụng" (Exit app). Screenshot: cashier-shift-gate.png. PASS.
65. Attempted to close the forced gate via the window's X button: blocked — gate stayed open (the `Closing` handler's `e.Cancel=true` for `forceOpen` correctly fired). PASS.
66. Opened shift (300,000đ), then checked MainWindow menu: only **"Bàn ăn"** and **"Ca làm việc"** visible (plus the OS "System" menu) — Kitchen/Thực đơn/Kho nguyên liệu/Báo cáo/Lịch sử ca all correctly hidden. PASS.
67. Full Order→Payment cycle as Cashier (T6, Spring Rolls x1, Cash 45,000 exact): completed successfully, `Payment` correctly stamped with **cashier1's own shift ID** (not admin's). Confirms the role works end-to-end for its core job, not just menu-gating. PASS.
68. Exit-app button on a fresh gate (no shift open): closed cashier1's shift via SQL first to get a clean re-test, relaunched, logged in as cashier1 again, clicked "Thoát ứng dụng" — process terminated cleanly, no orphaned window or process left running. PASS.

## Session 3 — KitchenStaff (kitchen1/Kitchen@123)

69. Login as KitchenStaff: **no shift gate at all** (only MainWindow appears — `RequiresShiftGate`/`CanSeeShift` are both false for this role, so `MainWindow_ContentRendered` returns before constructing the gate). PASS.
70. MainWindow menu: only **"Bếp"** visible (plus OS "System" menu) — everything else (Bàn ăn, Ca làm việc, Thực đơn, Kho nguyên liệu, Báo cáo, Lịch sử ca) correctly hidden. PASS.
71. Kitchen screen access: opened cleanly, no crash. Queue was empty at the time (no pending items in the DB from prior sessions — everything had already been marked Done or paid) — didn't get to exercise a fresh mark-in-progress/mark-done click in this role specifically, but the same code path was already confirmed working under the Admin session (rows 58-59). Confirms the role reaches the screen correctly; the interaction itself is already verified functionally under a different login.

## Session 1 Admin — Shift History (Admin-only)

60. List closed shifts, default 7-day filter (7/24–7/30): shows shift #1 (closed, -100 opening) correctly; the currently-open shift #2 is correctly excluded from the list. PASS.
61. Midnight-spanning shift bucketing: inserted a synthetic closed shift directly via SQL (`OpenedAt='2026-07-29 23:45'`, `ClosedAt='2026-07-30 00:15'`) to test the date-bucketing query without waiting for a real midnight. With the wide 7/24–7/30 filter, it appears correctly. Narrowed the filter to **From=To=7/30 only** — the midnight-spanning shift correctly **disappeared** (bucketed only under its open-date 7/29, not double-counted on the close-date 7/30 too). Matches the documented "ca tính theo ngày mở ca" design exactly. PASS. Screenshots: shifthistory-midnight-span.png, shifthistory-jul30-only.png.
62. Print report from a selected row: button correctly disabled with no selection, enabled after selecting a row, PDF opened with no crash. PASS.

## Session 1 Admin — Menu Management

18. Add category duplicate case-insensitive ("APPETIZERS" vs "Appetizers"): blocked, "Không thể thêm danh mục — tên có thể đã tồn tại.", category count stayed 3. PASS.
19. Add item duplicate case-insensitive ("spring rolls" vs "Spring Rolls"): blocked, same pattern. PASS.
20. Add item price = 0: blocked, "Giá món phải lớn hơn 0." PASS.
21. Add item price = -100: blocked, same message. PASS.
22. **Add item "Spring Rolls " (trailing space) while "Spring Rolls" already exists: NOT blocked — FAIL, bug confirmed.** New row created (MenuItemId 5, ItemName="Spring Rolls " with trailing space, Price 46000), visually indistinguishable from the original in the DataGrid (no trim-on-display either). Root cause pinned via source: `MenuItemService.IsDuplicateName` (`MenuItemService.cs:39-41`) does `string.Equals(i.ItemName, name, StringComparison.OrdinalIgnoreCase)` — exact char comparison, no `.Trim()` anywhere in the ViewModel or Service. Severity: **major** — this is the exact class of bug the recent commit (`1033c16`, "add duplicate-name validation for menu items/categories") was supposed to close, and a single trailing space defeats it entirely, so the fix doesn't actually guarantee menu-name uniqueness. Cleaned up: deleted the junk row afterward (confirmed back to 4 items).
23. Add "Iced Tea" under a different category ("Appetizers") while "Iced Tea" already exists under "Beverages": blocked — confirms name uniqueness is **global**, not per-category, matching code (`IsDuplicateName` queries `GetMenuItems()` with no category filter). Not necessarily a bug — flagging as a **product decision for Boss to confirm**: is cross-category name reuse (e.g. two different "Trà đá" in different sections) intentionally disallowed, or should it be allowed?
24. Update "Spring Rolls" keeping its own name unchanged: succeeds, no false "duplicate" error (exclude-self logic works). PASS.
25. Delete a never-ordered item (the "Spring Rolls " junk row): confirm dialog → Yes → deleted successfully, grid back to 4 rows. PASS.
26. Delete category containing items ("Appetizers", which has "Spring Rolls"): confirm dialog → Yes → blocked, "Không thể xóa — danh mục đang có món ăn.", category still exists. PASS.
27. Add category "Desserts" (happy path) then delete it while empty (happy path): both succeeded cleanly, category list back to 3 (Appetizers, Main Course, Beverages) afterward. PASS.
28. Delete item that was previously ordered ("Chicken Rice", now referenced by OrderItems from T1's paid order): confirm dialog → Yes → blocked, "Không thể xóa — món đang được dùng trong đơn hàng.", item still exists. Confirms the FK `Restrict` applies even to an item from an already-Paid order (permanent lock, no hard-delete path exists — matches recon). PASS.
29. **10,000-character item name: FAIL — misleading error message, bug confirmed.** UI showed "Không thể thêm món — tên có thể đã tồn tại." (implies a duplicate-name rejection), but the real cause is completely different: `ItemName` column is `nvarchar(150)`; the oversized insert throws `SqlException: String or binary data would be truncated in table 'MenuItems', column 'ItemName'`. Root cause pinned independently via `error.log` (not just re-clicking): `MenuItemDAO.SaveMenuItem` (`MenuItemDAO.cs:27-41`) catches **any** `Exception` generically, logs it, and returns `false`; `MenuManagementViewModel.AddMenuItem` (`MenuManagementViewModel.cs:128-138`) maps **every** `false` from the service to the same hardcoded "tên có thể đã tồn tại" message regardless of actual cause. No crash, no data corruption — app stayed responsive — but the error message actively misleads the user about why their (unique, valid-looking) name was rejected. Severity: **minor** (wrong message, not wrong behavior — the truncation is correctly rejected, just mislabeled). Same generic-catch-then-mislabel pattern independently observed in Inventory's concurrency-edit case (recon note) — likely systemic across the DAOs that follow this template, not a one-off; not chasing every occurrence given time budget, but worth a dev-side note.



## Cross-cutting

72. DB unavailable at login: took `RestaurantPOSDb` genuinely offline (`ALTER DATABASE RestaurantPOSDb SET OFFLINE WITH ROLLBACK IMMEDIATE`) — note: simply stopping the LocalDB instance via `sqllocaldb stop` does NOT work for this test, LocalDB auto-restarts on the next connection attempt (`Auto-create: Yes`), so a real "DB down" simulation needs the database itself offline, not just the instance stopped. With the DB offline, clicked Login (admin/Admin@123): app stayed responsive throughout, generic MessageBox "Đã xảy ra lỗi không mong muốn. Vui lòng thử lại." shown (title "Lỗi"), Login button correctly re-enabled after (`IsEnabled=True`), no crash, no hang. Confirmed independently via `error.log`: full `SqlException` (transient-failure / login-failed-for-database) logged under `[UnhandledException]`, caught by `App.xaml.cs`'s global `Application_DispatcherUnhandledException` handler (`e.Handled = true`). PASS — this is exactly the graceful-degradation behavior you'd want. Restored DB online immediately after (`ALTER DATABASE RestaurantPOSDb SET ONLINE`, verified `state_desc = ONLINE`).
73. DB unavailable mid-session, right at the post-login shift-gate moment: **not live-reproduced (race window is sub-100ms, out of reach for tool calls with process/network round-trip overhead), but root cause independently confirmed via source read** — flagging as a bug candidate rather than a confirmed live FAIL. `MainWindow.xaml.cs:22-36` (`MainWindow_ContentRendered`): sets `_shiftGateShown = true` **before** calling `viewModel.RefreshShift()`, and neither `MainWindow_ContentRendered` nor `MainWindowViewModel.RefreshShift()` (`MainWindowViewModel.cs:58-65`) has any try/catch around the DB call (`_shiftService.GetOpenShift(_userId)`). If that call throws (DB blip at exactly this moment), the exception propagates out of `ContentRendered`, gets caught only by the app-global `DispatcherUnhandledException` handler (generic MessageBox, `Handled=true`, no crash) — but `_shiftGateShown` is already latched `true`, and `ContentRendered` only fires once per window, so **the shift-open gate never appears again for that session**, even for a Cashier where `RequiresShiftGate=true` (mandatory, no skip button). Net effect: a Cashier could end up inside MainWindow able to process cash payments without ever having opened a shift, purely from bad timing on a transient DB error. Matches the code's own comment acknowledging this exact risk (`MainWindow.xaml.cs:19-21`: "worst case the gate silently doesn't show"). Severity: **major** if confirmed (bypasses a business-integrity gate: shift reconciliation assumes every cash-handling session opened a shift) — recommend Boss/dev treat this as a real finding despite the lack of a live click-through repro; the mechanism is unambiguous from the code and self-documented by the original author's own comment.
74. Launch the built .exe from a different working directory (`C:\Users\Rabuno\Documents`, not its own `bin/Debug/net10.0-windows` folder): app started normally, login (admin/Admin@123) succeeded, MainWindow appeared correctly connected to the real `RestaurantPOSDb`. PASS today — but this is a coincidence, not a guarantee: `AppDbContext.GetConnectionString()` (`AppDbContext.cs:38-46`) resolves `appsettings.json` via `Directory.GetCurrentDirectory()` (not `AppContext.BaseDirectory`), and when the file isn't found at that path (`optional: true`), it falls back to a **hardcoded default connection string** that happens to be textually identical to today's `appsettings.json` value. **Polish/fragility note, not a bug today**: if a dev ever changes the real `DefaultConnectionString` in `appsettings.json` (e.g., pointing at a different DB name or server) without updating the hardcoded fallback in `AppDbContext.cs:45` to match, launching the exe from any working directory other than its own folder (e.g., via a desktop shortcut with the wrong "Start in" path, or a Task Scheduler entry) would silently connect to the **wrong/stale database** with zero error shown to the user. Recommend switching to `AppContext.BaseDirectory` for `SetBasePath` so this class of risk disappears entirely, independent of whether the fallback string is kept in sync.
75. Keyboard-only navigation, Login → MainWindow → order flow (Tab/Enter only, no mouse): Login (cashier1/Cashier@123) worked cleanly — Tab moves correctly between Username → Password → (external), `IsDefault="True"` on the Login button means Enter submits from the password field. Note: the test sandbox has a Vietnamese Telex IME active system-wide, which silently mangles typed sequences like "as" → "á" when injected via `SendKeys` — this is an environment artifact of the machine used for testing, not an app bug (worked around by pasting text via clipboard instead of simulating keystrokes for the actual credential values, while still using real `{TAB}`/`{ENTER}` key sends for navigation/submission). Shift-open gate (`ShiftWindow`, mandatory for Cashier): Tab order confirmed clean and complete (ScheduledStart → ScheduledEnd → "Lưu giờ quy định" → OpeningCash → "Mở ca"), and pressing **Enter while a plain `Button` (no `IsDefault`) has keyboard focus successfully invokes it** — standard WPF behavior, confirmed live. `MainWindow`'s top menu bar has no XAML-declared accelerator keys (no `_` mnemonics on any `Header`), but **F10 correctly activates the native `Menu` control** and focuses the first item; Enter then invokes it. **FAIL — bug confirmed live**: inside `TableMapWindow` ("Sơ đồ bàn"), **Tab never reaches an individual table tile** — it only cycles between the "Làm mới" button and one unnamed `List` container; the table tiles themselves (`Border` + `MouseLeftButtonDown`, per earlier recon of `TableMapWindow.xaml`) are not part of the tab order and have no keyboard-invoke path at all (confirmed by direct Tab-stepping, not just code reading). Severity: **major** — a keyboard-only user (assistive tech, broken mouse, or simply Tab-driven habit) cannot select any table, and therefore cannot reach Order or Payment at all; this isn't a degraded flow, it's a fully blocked one. Scope note for the report: this project has no stated accessibility requirement, so Boss may choose to treat this as lower priority than the severity label implies in a strict WCAG context — flagging the tension explicitly rather than silently downgrading it myself.
76. Holistic review of `error.log` (all 5 entries across the full run, from file start to now): every entry accounted for — (1) `MenuCategoryDAO.DeleteCategory` FK conflict deleting a category with items (expected, matches row 26's PASS), (2) `MenuItemDAO.SaveMenuItem` truncation on the 10,000-char name (row 29's bug, already logged), (3) `IngredientDAO.DeleteIngredient` FK conflict (expected, matches Inventory FK-delete-blocked row), (4) `MenuItemDAO.DeleteMenuItem` FK conflict deleting an ordered item (expected, matches row 28's PASS), (5) the DB-offline login attempt from row 72 above. No orphan/unexplained exceptions found. PASS (nothing new surfaced).
