<Window x:Class="RestaurantPOS.WpfApp.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RestaurantPOS.WpfApp.ViewModels"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Báo cáo doanh thu" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:DashboardViewModel />
    </Window.DataContext>
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Từ:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding FromDate}" Margin="0,0,16,0" />
            <TextBlock Text="Đến:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding ToDate}" Margin="0,0,16,0" />
            <Button Content="Làm mới" Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Text="Top món bán chạy" FontWeight="Bold" Margin="0,0,0,8" />
        <DataGrid DockPanel.Dock="Top" ItemsSource="{Binding TopSellers}" AutoGenerateColumns="False" IsReadOnly="True"
                  Height="140" Margin="0,0,0,16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Món" Binding="{Binding ItemName}" Width="*" />
                <DataGridTextColumn Header="SL bán" Binding="{Binding QuantitySold}" Width="100" />
                <DataGridTextColumn Header="Doanh thu" Binding="{Binding Revenue, StringFormat=N0}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <lvc:CartesianChart Series="{Binding Series}" XAxes="{Binding XAxes}" />
    </DockPanel>
</Window>
