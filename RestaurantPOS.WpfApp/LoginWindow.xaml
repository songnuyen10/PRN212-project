<Window x:Class="RestaurantPOS.WpfApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RestaurantPOS.WpfApp.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Restaurant POS — Login" Height="360" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>
    <StackPanel Margin="32" VerticalAlignment="Center">
        <TextBlock Text="Restaurant POS" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,24" />

        <TextBox x:Name="TxtUsername"
                 materialDesign:HintAssist.Hint="Username"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,16" />

        <PasswordBox x:Name="TxtPassword"
                     materialDesign:HintAssist.Hint="Password"
                     Margin="0,0,0,16" />

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8" />

        <Button Content="Login" Click="Login_Click" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" />
    </StackPanel>
</Window>
